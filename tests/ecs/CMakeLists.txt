SET(
  CMAKE_RUNTIME_OUTPUT_DIRECTORY
  ${CMAKE_RUNTIME_OUTPUT_DIRECTORY}/tests
)
LINK_LIBRARIES(gtest gmock pthread)

ADD_EXECUTABLE(ecs ecs.cpp)
ADD_TEST(
  NAME ecs
  COMMAND ${CMAKE_RUNTIME_OUTPUT_DIRECTORY}/ecs
)

